# 2026-05-09

- Fixed Accounts/UnifiedTimeline inbox 404: frontend calls `GET /api/v1/emails/threads?...`, but backend had inbox routes only mounted at `/api/v1/integrations/email/inbox`. Added shared inbox route registration and mounted it under `/api/v1/emails` alongside existing compose routes, preserving the integrations alias.
- Indexed 44 legacy CRM screenshots from `docs/screenshots_original_UIUX` into `docs/original-crm-screenshot-index.md`, covering navigation, admin/security, module customization, automation, portal/email config, mappings, tax, terms, calendar, and mail converter functions. The screenshot folder is gitignored, so the tracked index lives one level up in `docs/`.
- Swept major frontend API clients for route/verb drift. Added compatibility for SLA `PATCH /sla-policies/{id}`, KB `GET /kb/articles/suggest`, billing `/billing/plans` and `/billing/subscription/cancel`, checkout `tier` payloads, and public KB `/api/public/kb/...` routes.
- Evaluated the screenshot-derived legacy CRM surface against current implementation and wrote `docs/original-crm-feature-implementation-status.md`. Current repo has a full modern CRM core, while most legacy admin/configuration screens remain partial, backend-only, or absent.
- Created `docs/original-crm-feature-prompts.md`, an agent-ready prompt pack with verification prompts for FULL features and implementation prompts for partial/missing legacy CRM features.
- Created 64 GitHub issues in `SEI-Mkittok/omnir-refactor` from the CRM feature prompt pack: #230-#253 for QA/hardening, #254-#290 for feature implementation gaps, and #291-#293 for planning epics. Added `docs/original-crm-feature-issues.md` as the local issue map.
- Replaced the CRM epic issue structure with GitHub milestones: `CRM Admin Configuration` (#3 milestone, 35 issues), `CRM Inventory And Finance` (#1 milestone, 16 issues), and `CRM Automation And Intake` (#2 milestone, 10 issues). Removed partial sub-issue links from #291, closed #291-#293 as superseded, and updated `docs/original-crm-feature-issues.md`.
- Executed CRM Admin Configuration Bundle 0 + Bundle 1 kickoff:
  - Closed issue #230 after confirming PR #295 merged and passed CI.
  - Created milestone `CRM Admin Configuration Advanced` (#4) and moved advanced-only issues #264 and #288 out of core scope.
  - Added execution-order labels/comments (`bundle-0`..`bundle-6`) and `core-scope`/`advanced-scope` tagging across milestone issues; re-scoped #270 to numbering hardening parity work instead of missing-route work.
- Implemented settings/integrations parity work:
  - Added shared frontend calendar API client (`web/src/api/calendar.ts`).
  - Refactored `CalendarPage` to use shared calendar API.
  - Upgraded `IntegrationsSettingsPage` to treat Google/Outlook calendar as active integrations with real OAuth connect (`/api/v1/calendar/auth/*`), disconnect (`/api/v1/calendar/connections/{id}`), and sync (`/api/v1/calendar/sync`) actions.
  - Kept unsupported providers clearly marked as `Coming soon`.
  - Expanded frontend contract tests for integration disconnect/credential-clear and calendar connection/sync route contracts.
