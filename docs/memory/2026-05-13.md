# 2026-05-13

- Created GitHub issue #304: visual drag-and-drop role hierarchy for tiered permissions, based on Matthias's live vtiger-style hierarchy example. Labeled `enhancement` and `core-scope`.
- Created GitHub issue #305: complete sharing rules implementation and enforcement. Called out that current implementation appears to be module defaults + broad role/group grants, not full vtiger-style source/target advanced sharing rules.
- Created GitHub issue #306: Groups settings page crashes when groups response is null. Likely root cause is backend returning nil slice as `{"data":null}` when no groups exist, then frontend reads `groups.length`.
- Created GitHub issue #307: extend custom fields support to quotes. Current custom field entity whitelist excludes quotes and quote models/table do not expose `custom_fields`.
- Created GitHub issue #308: extend custom fields support to Knowledge Base articles. Current custom field entity whitelist excludes KB and `articles` do not expose `custom_fields`; noted public help center should not leak internal fields by default.
- Created GitHub issue #309: improve Config Editor UX so administrators can understand support email, upload max, default page size, and preview length settings. Current UI uses placeholder-only labels, making loaded numeric fields ambiguous.
- Created GitHub issue #310: SLA policy creation rejects valid fractional response times such as `0.25`. Likely frontend `min={0.1}` + `step={0.5}` browser validation mismatch with backend positive-float model.
- Created GitHub issue #311: show usernames/display names in Audit Log actor column, while preserving raw actor IDs for debugging and CSV export.
- Created GitHub issue #312: Audit Log search field cannot accept typed input. Likely cause is `AuditLogPage` passing `searchValue=""` and a no-op `onSearchChange` to `FilterBar`.
- Created GitHub issue #313: move admin settings out of the persistent sidebar/menu and reorganize the Settings hub into clearer categories while preserving permissions and deep links.
- Created GitHub issue #314: make sidebar menu section titles collapsible so users can hide/show grouped navigation areas and reduce visual clutter.
- Implemented Bundle 5 CRM Admin Configuration locally: module layouts, module relationship definitions, generic custom relationships via crm_entity_links, admin/runtime APIs, frontend settings pages, and layout-aware CRM forms/details. Validation passed: api go test ./..., web npm run typecheck, web npm test -- --run, and scripts\pre-push-check.cmd.
