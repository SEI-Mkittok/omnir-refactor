# 2026-05-09

- Fixed Accounts/UnifiedTimeline inbox 404: frontend calls `GET /api/v1/emails/threads?...`, but backend had inbox routes only mounted at `/api/v1/integrations/email/inbox`. Added shared inbox route registration and mounted it under `/api/v1/emails` alongside existing compose routes, preserving the integrations alias.
- Indexed 44 legacy CRM screenshots from `docs/screenshots_original_UIUX` into `docs/original-crm-screenshot-index.md`, covering navigation, admin/security, module customization, automation, portal/email config, mappings, tax, terms, calendar, and mail converter functions. The screenshot folder is gitignored, so the tracked index lives one level up in `docs/`.
- Swept major frontend API clients for route/verb drift. Added compatibility for SLA `PATCH /sla-policies/{id}`, KB `GET /kb/articles/suggest`, billing `/billing/plans` and `/billing/subscription/cancel`, checkout `tier` payloads, and public KB `/api/public/kb/...` routes.
